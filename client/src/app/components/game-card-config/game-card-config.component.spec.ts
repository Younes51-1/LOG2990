import { TestBed } from '@angular/core/testing';
import { GameCardConfigComponent } from './game-card-config.component';

describe('GameCardComponent', () => {
    beforeEach(async () => {
        await TestBed.configureTestingModule({
            declarations: [GameCardConfigComponent],
        }).compileComponents();
    });
});
