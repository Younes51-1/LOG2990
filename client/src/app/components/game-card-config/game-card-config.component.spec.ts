import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Vec2 } from '@app/interfaces/vec2';
import { GameCardConfigComponent } from './game-card-config.component';

describe('GameCardComponent', () => {
    let component: GameCardConfigComponent;
    let fixture: ComponentFixture<GameCardConfigComponent>;

    beforeEach(async () => {
        await TestBed.configureTestingModule({
            declarations: [GameCardConfigComponent],
        }).compileComponents();
    });


});
